# WebUI 使用说明

## 功能简介

WebUI 可以理解为原本两个命令行程序 `XDUClassVideoDownloader.py` 和 `Automation.py` 的网页整合版：既能按 `LiveID` 下载单门课程，也能读取或生成 `automation_config.ini` 批量下载课程。

在此基础上，WebUI 还增加了本地视频查看功能，可以在网页中同时播放同一节课的 `pptVideo` 和 `teacherTrack` 两个视角，并在视频下方显示字幕。

WebUI 复用现有下载和认证流程，不需要另外维护一套配置。

## 环境准备

### Windows 用户看这里！（懒人包）

Windows 用户可以直接从项目的 [Releases](https://github.com/lsy223622/XDUClassVideoDownloader/releases/latest) 页面下载打包好的程序。

打包版本已经包含运行所需组件，开箱即用：

- 不需要安装 Python
- 不需要手动安装 Python 依赖
- 不需要另外准备 FFmpeg

下载后直接运行 WebUI 对应的程序即可。

### 通过 Python 脚本运行

1. 安装 Python 依赖：

   ```shell
   pip install -r requirements.txt
   ```

2. 如果使用的是源码版，并且需要下载 2024 学年及以前课程，或需要合并视频，请按 `README.md` 中的说明准备 FFmpeg。

3. 如果此前没有配置过登录信息，可以启动 WebUI 后在 `设置` 页面完成配置。

## 启动 WebUI

### Windows 源码版用户

双击或运行：

```shell
webui.bat
```

### Python 脚本运行

```shell
python WebUI.py
```

启动后浏览器会自动打开 WebUI。默认地址是：

```text
http://127.0.0.1:5050/
```

如果 `5050` 端口已被占用，程序会自动尝试后续端口。请以控制台中打印的 `WebUI 正在启动: ...` 地址为准。

### 启动参数

```shell
python WebUI.py [--host HOST] [--port PORT] [--no-browser]
```

- `--host HOST`：监听地址。默认读取 `auth.ini` 中的 `WEBUI.bind_host`；如果未配置，则使用 `127.0.0.1`。
- `--port PORT`：起始端口，默认 `5050`。
- `--no-browser`：启动后不自动打开浏览器。

示例：

```shell
python WebUI.py --port 5060
python WebUI.py --host 0.0.0.0 --port 5050 --no-browser
```

## 页面说明

WebUI 左侧菜单包含三个页面：

- `下载`：单课程下载、批量下载、查看下载输出
- `查看`：播放本地已下载课程视频
- `设置`：配置登录方式、扫码登录、设置 WebUI 访问密码

如果 WebUI 检测不到可用登录信息，会自动切换到 `设置` 页面并显示初始设置提示。

## 设置页面

建议首次使用时先进入 `设置` 页面完成登录配置。

### 登录配置

WebUI 支持以下认证方式：

1. `统一身份认证`
   - 填写西电统一身份认证账号和密码。
   - 与命令行中的统一身份认证登录使用同一套配置。

2. `超星账号密码`
   - 填写超星账号和密码。
   - 不是西电统一身份认证账号密码。

3. `手动 Cookie`
   - 填写 `_d`、`UID`、`vc3`。
   - Cookie 获取方法见 `AUTHENTICATION.md`。

4. `学在西电扫码登录`
   - 适合不想手动复制 Cookie 的情况。
   - 可直接使用右侧的扫码登录功能。

填写完成后点击 `保存登录配置`。

注意：

- 已保存的账号、密码和 Cookie 不会明文显示在网页中，而是显示为 `********`。
- 如果某个已保存字段不需要修改，保持原样即可。
- 如果点击字段并清空掩码，需要重新填写该字段。
- 取消勾选 `保存认证信息` 后保存，会清除本地保存的认证字段。

### 学在西电扫码登录

1. 在 `设置` 页面点击 `生成二维码`。
2. 等待二维码显示。
3. 使用手机学在西电 App 扫码。
4. 在手机端确认登录。
5. 页面提示登录成功后，WebUI 会自动把 `_d`、`UID`、`vc3` 保存到 `auth.ini`。

扫码登录成功后，认证方式会保存为 `学在西电扫码登录`。

注意：

- 二维码有有效期，超时后请重新生成。
- 切换离开 `设置` 页面时，当前二维码任务会被取消。
- 扫码登录不会保存账号密码，只会保存本次登录获取到的 Cookie。

### WebUI 访问密码

`WebUI 访问密码` 用于限制进入网页界面的人。设置后，打开 WebUI 时需要先输入该密码。

设置方法：

1. 在 `设置` 页面找到 `WebUI 访问密码`。
2. 输入新密码和确认密码。
3. 点击 `保存访问密码`。

清除方法：

1. 点击 `清除密码`。
2. 刷新或重新打开 WebUI 后，将不再要求输入访问密码。

注意：

- WebUI 访问密码不是西电密码，也不是超星密码。
- 密码会以哈希形式保存到 `auth.ini`，不会明文保存。
- 如果 `auth.ini` 中设置了 `WEBUI.allow_password_change = false`，网页中不会显示修改访问密码的面板。

## 下载页面

下载页面分为 `单课程下载`、`批量下载` 和 `命令行输出` 三部分。

### 单课程下载

适合只下载某一门课或某一节课。

使用步骤：

1. 填写课程 `LiveID`。
2. 选择下载模式：
   - `全部视频`
   - `单节课`
   - `半节课`
3. 选择视频类型：
   - `两种都下载`
   - `仅 pptVideo`
   - `仅 teacherTrack`
4. 如需跳过部分周数，在 `跳过周数` 中填写，例如：
   - `5`
   - `1-3`
   - `1,3,5`
   - `1-3,7,9-11`
5. 按需选择是否 `自动合并相邻节次`。
6. 点击 `开启单课程下载`。

下载开始后，输出会显示在下方 `命令行输出` 面板中。

### 批量下载

批量下载依赖 `automation_config.ini`。

#### 首次生成批量配置

如果当前目录中没有 `automation_config.ini`，WebUI 会显示生成配置的表单。

使用步骤：

1. 填写超星 `用户 ID`。
2. 选择学年和学期。
3. 选择默认视频类型。
4. 点击 `生成配置文件`。
5. 等待课程扫描完成。
6. 在课程列表中勾选需要下载的课程。
7. 点击 `开启批量下载`。

生成配置后，WebUI 会把课程列表写入 `automation_config.ini`。

#### 使用已有批量配置

如果已经存在 `automation_config.ini`，WebUI 会直接显示课程列表。

可用操作：

- `读取配置`：重新读取本地 `automation_config.ini`。
- `刷新课程`：按配置中的 UID、学年和学期重新扫描课程。
- `全选`：勾选所有课程。
- `全不选`：取消勾选所有课程。
- `保存当前选择到配置文件`：把当前勾选状态写回 `automation_config.ini`。
- `开启批量下载`：下载当前勾选的课程。

注意：

- 点击 `开启批量下载` 时，只会下载页面中当前勾选的课程。
- 如果只是临时勾选后直接下载，不点击 `保存当前选择到配置文件`，配置文件中的 `download` 字段不会被持久修改。
- WebUI 同一时间只允许运行一个下载任务。

### 命令行输出

WebUI 会把下载过程中的命令行输出显示在 `命令行输出` 面板中。

可用操作：

- `清空`：清空当前页面中的输出内容。
- 页面刷新后，如果下载任务仍在运行，WebUI 会尝试恢复输出显示。

## 查看页面

查看页面用于播放本地已经下载好的视频。

### 视频列表

1. 点击左侧菜单的 `查看`。
2. 页面会自动扫描本地已下载视频。
3. 左侧显示课程列表和节次列表。
4. 点击某一节课后，右侧会显示可播放的视频。

如果刚完成下载，可以点击 `刷新` 重新扫描本地视频。

### 支持的文件

查看页面会扫描程序目录下的下载文件，支持：

- 视频：`.mp4`、`.ts`
- 字幕：`.srt`、`.vtt`

课程和节次识别依赖程序生成的文件名。请尽量不要手动修改已下载视频或字幕的文件名，否则可能导致查看页面无法识别。

### 播放控制

查看页面使用统一的播放器控制栏，不使用浏览器原生视频控制条。

支持的操作：

- 播放 / 暂停
- 静音 / 取消静音
- 拖动公共进度条跳转播放位置
- 调整播放速度
- 调整 `pptVideo` 时间偏移
- 调整字幕时间偏移
- 调整字幕字号
- 开启或关闭字幕粗体
- 单个视频全屏
- 最大化视频区域

如果同一节课同时存在 `pptVideo` 和 `teacherTrack`，页面会同时显示两个视频，并使用公共进度条同步播放。

默认优先播放 `teacherTrack` 的声音。也可以点击视频标题栏中的静音按钮切换当前有声音轨；同一时间只保留一个视频出声。

字幕显示在视频下方的字幕块中，不会覆盖在视频画面上。

### 偏移调整

如果两个视角略有不同步，可以使用 `视频时间偏移` 中的 `PPT 偏移（秒）` 调整 `pptVideo`。

如果字幕和视频不同步，可以使用 `字幕设置` 中的 `字幕偏移（秒）` 调整字幕时间。

偏移可以输入数字，也可以拖动滑条调整。

## 局域网访问

默认情况下，WebUI 只监听 `127.0.0.1`，只能本机访问。

如果需要让局域网其他设备访问，可以使用：

```shell
python WebUI.py --host 0.0.0.0
```

也可以在 `auth.ini` 中配置：

```ini
[WEBUI]
bind_host = 0.0.0.0
allowed_clients = 192.168.1.0/24,127.0.0.1/32
```

说明：

- `bind_host` 控制默认监听地址。
- `allowed_clients` 用于限制允许访问的客户端 IP 或网段，多个值可用逗号或空白分隔。
- 如果 `allowed_clients` 为空，则不按客户端 IP 限制访问。

强烈建议局域网访问时同时设置 WebUI 访问密码。

## 配置文件说明

WebUI 会使用或维护以下文件：

| 文件/目录 | 说明 |
| --- | --- |
| `auth.ini` | 保存认证方式、账号信息、Cookie、WebUI 访问密码和访问控制设置。 |
| `automation_config.ini` | 保存批量下载课程列表、学期参数、视频类型和课程下载选择。 |
| `logs/` | 保存日志和扫码登录二维码等临时文件。 |
| `WebUI.py` | WebUI 启动入口。 |
| `webui/static/` | WebUI 前端页面文件。 |

注意：`auth.ini` 可能包含敏感信息，请不要分享给他人，也不要提交到公开仓库。

## 注意事项

1. **先配置登录信息**

   - 如果没有可用登录信息，下载和课程扫描可能失败。
   - 推荐优先使用 `学在西电扫码登录`。

2. **同一时间只运行一个下载任务**

   - 如果已有下载任务正在运行，需要等待结束后再启动新的任务。

3. **不要随意改名下载文件**

   - 查看页面依赖文件名识别课程、日期、节次和视频轨道。

4. **WebUI 不等于公网服务**

   - 默认只建议本机使用。
   - 如果开放到局域网，请设置访问密码并限制客户端网段。

5. **FFmpeg 按运行方式区分**

   - Release 打包版已经包含所需组件，不需要另外准备 FFmpeg。
   - Python 源码版需要合并或下载旧学年课程时，请按 `README.md` 准备 FFmpeg。

## 故障排除

### 浏览器没有自动打开

查看控制台输出的地址，手动复制到浏览器打开。如果启动时使用了 `--no-browser`，这是正常现象。

### 打开页面后要求输入 WebUI 访问密码

说明当前已经设置过 WebUI 访问密码。输入该密码即可进入。

如果忘记密码，可以关闭 WebUI 后手动编辑 `auth.ini`，删除 `[WEBUI]` 段中的 `password_hash` 或旧版 `password` 字段。

### 页面提示没有可用登录信息

进入 `设置` 页面，使用统一身份认证、超星账号密码、手动 Cookie 或学在西电扫码登录完成配置。

### 扫码登录失败或超时

1. 重新点击 `生成二维码`。
2. 确认使用的是学在西电 App 扫码。
3. 确认手机端已经点击确认登录。
4. 如果仍失败，尝试删除过期的 `auth.ini` 后重新配置。

### 生成批量配置失败

1. 检查 `用户 ID` 是否正确。
2. 检查学年和学期是否正确。
3. 确认当前登录信息有效。
4. 重新登录或重新扫码后再试。

### 下载失败

1. 检查网络连接。
2. 确认登录信息未过期。
3. 确认课程视频确实存在。
4. 查看 `命令行输出` 面板中的错误信息。
5. 必要时参考 `README.md` 和 `AUTHENTICATION.md` 重新配置。

### 查看页面找不到视频

1. 确认视频已经下载完成。
2. 点击 `查看` 页面中的 `刷新`。
3. 确认视频文件仍位于程序下载目录下。
4. 确认文件名没有被手动改掉。

### 字幕不显示

1. 确认同一课程目录下存在 `.srt` 或 `.vtt` 字幕文件。
2. 确认字幕文件名没有被手动修改。
3. 如果字幕时间不准，使用 `字幕偏移（秒）` 调整。

### 两个视频不同步

使用 `视频时间偏移` 中的 `PPT 偏移（秒）` 调整 `pptVideo`。调整后继续使用公共进度条播放和跳转。

### 局域网无法访问

1. 确认启动时使用了 `--host 0.0.0.0`，或在 `auth.ini` 中配置了 `WEBUI.bind_host = 0.0.0.0`。
2. 检查系统防火墙是否放行对应端口。
3. 如果配置了 `allowed_clients`，确认访问设备的 IP 在允许范围内。
